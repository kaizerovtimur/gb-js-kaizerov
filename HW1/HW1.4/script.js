"use strict";

let mode = "normal":  // Можно.
 
let my_value3 = 102; // Можно.

let 3my_value3 = "102"; // Нельзя, так как цифра не может стоять в начале.

let __hello__ = "world"; // Можно.

let $$$ = "money"; // Можно.

let !0_world = true; // Нельзя, так как разрешены только два знака, кроме букв и цифр (_ и $).

